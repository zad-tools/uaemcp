# @open-emirates/contracts

Dependency-free response-envelope and Schema 2.1 MCP tool-catalog contracts for
Open Emirates clients. External payloads can be checked with `parseEnvelope`,
`parseToolCatalog` and `parseToolCatalogEntry`.

Dependency-free TypeScript contracts for the stable Open Emirates response envelope.

This package contains no network client and no MCP implementation.
